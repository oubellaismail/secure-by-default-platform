# Falco

eBPF runtime detection (Week 6, scope-cut zone). Detects malicious behavior at runtime and emits security signals to observability.

Note: eBPF on local kind/k3s can have kernel/mount quirks — budget learning time.
